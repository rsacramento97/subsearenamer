# Current implementation status

The safety-critical copy engine is implemented in Rust. The repository currently has a web preview/core planning layer and a native desktop core; the production Windows shell is the remaining integration boundary.

Do not label the application production-ready until the Windows integration has passed the release checklist with real test files.
